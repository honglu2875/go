# Equal-work paired decoding: TPU qualification failed

The native exact-work implementation passed CPU qualification. Bfloat16 speculative execution failed the subsequent multi-host TPU numerical criterion, so the registered timing study did not start. No throughput or strength promotion follows from this study.

`BoundedTraceActors` owns exact game state, pending model/context tickets, an active mask and per-game remaining-move allowances. Inactive games retain their final state and episode identity. Terminal and move-limit outcomes take precedence over a work limit. State validation still stops before a capture-invalidated policy input can act. Six Rust trace tests and 21 Python native binding tests passed; model/decoder source remained identical to the earlier qualified board trace.

The CPU registration is `cpu_spec.json` (SHA256 `06ba4de5969d45d97477e4463ded3af3309f2e2eac5d44e3415c3cec05ca4e9a`). Source `8632ed1e8a4e7a2a2b68e304f1be201436619743dc6936569662f3264fba7bb8` ran once into `runs/trace-throughput-cpu-8632ed1e`. Every mode consumed 64 accepted moves for each of four games in both qualification and timing, with identical events and final native states. The single short CPU timing window took 1.7662 seconds sequentially, 3.1645 with learned behavior k=4, and 0.7830 with known-policy joint k=1. These are qualification observations, without a registered speed promotion. `cpu_result.json` independently audits event reconstruction, work/byte counters, timings, source/native identity and loaded parameter elements (analysis `8f7fab6e…`, result SHA256 `75155dc578c8e1578e2246ad7134dfe8665ac74c2bd1a5861c2e9fc5e1cc8c28`).

The TPU registration is `tpu_qualification_spec.json` (SHA256 `797d3797d26a03e13f16a64ee2a0d76f60006599794402826ed78d509dedc314`). Source `59aac28121bd9231d33e8f1873a338c0eafc9e4960b58093259c180b94453c9c` ran once as `pod-20260911T175419Z-fecd570e`. all configured hosts completed the three qualification modes, each consuming 128 moves/game across 64 games/host: 98,304 moves total. The learned-behavior mode diverged in 17 of 256 game streams and the known-policy joint mode in 28 of 256. Every first difference occurred at depth 1, 2 or 3 within a packet; none occurred at depth 0. all configured hosts failed the exact-stream/final-state criterion and stopped before timing.

`tpu_qualification_failure.json` independently checks every raw segment and the failure/stop condition. Its SHA256 is `1ebde874c22e7e6a199f5f576f1481d29af9cdf2e4cc31e00085fd1702a8db0b`, from analyzer source `c252002442d252fdad706ddf6ce7e41f1eaff81f7c501d0f443e7ea0886fad6e`. The attempt lasted 28.3599 seconds / 0.126044 chip-hours. Those execution costs are a subset of the reservation ledger, which also charges the observed engineering and idle window. Native build failures occurred before experiments and are retained in their build logs.

The first-difference pattern suggests that incremental versus full-prefix arithmetic matters on TPU. The records do not retain logits and cannot prove the cause or quantify logit errors. `trace_precision` is a separate complete clone testing float32 activations plus highest matmul precision with the same weights. Its sequential and speculative paths must qualify against each other on their actual devices. The failed bfloat16 evidence and its unchanged threshold remain here.

The model is the fixed 939,968-parameter exact-board student `c8653227…`, which failed its real-KataGo strength criterion. The known-policy control shares both actual policy draws; a behavior forecast receives less information. Neither executes a sequence of MCTS decisions. The resolver is currently serial, and logical transfer/compiler counters do not establish hardware traffic or MFU.

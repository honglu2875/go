The first student learns both prediction objectives, but its weak state tracking, small behavior-head advantage and first four KataGo losses did not justify a larger training run. Learned policy execution and the causal MCTS bridge are qualified. The subsequent explicit-board input study improved prediction but failed its external criterion: one historical-KataGo win, 56 losses and seven caps, plus eight strong-anchor losses. See `../board_state_distillation`. Investigate the observed near-certain-loss endgame continuations before increasing student training. Systems studies must preserve the information and work distinctions established below.

1. Completed: the fixed final student passed paired decoding against sequential controls in both greedy and stochastic play. Four learned behavior forecasts resolve 1.7051 / 1.3066 moves per game per dispatch, compared with 1.0566 / 1.0762 using initialized behavior parameters. Known-policy independent stochastic forecasts reach 1.3867; using actual known self-play draws reaches 3.0039 with one sample. Exact actions, resets and terminal scores are preserved. See `../learned_trace`. This is policy-only, with full-history prefill and a serial native resolver. Timings are not a repeated matched-work TPU benchmark; no MCTS-equivalence claim follows from this result.

2. Improve forecast coverage before increasing model size. At the first opponent turn, forecasts share the same causal prefix within each player view, so sampling without replacement can spend the fixed four-branch budget on distinct moves. Preserve independent future samples after prefixes diverge and preserve the own-policy random draw keyed by episode/absolute ply. Compare against independent sampling at fixed work; never select a branch using knowledge of future actual moves. A separate control can supply an exact native table of legal replies after each root action. The TPU indexes that table after its root prediction, avoiding another CPU exchange. Charge all native table construction and transfer bytes; retain a resource cap because the table is quadratic in action count.

3. Completed input ablation: an exact per-position board encoder reduced expert-policy KL by 57.55% and illegal probability by 96.07% against identical empty-board inputs. The two arms shared initial parameter elements, sampler and data. The canonical history trunk stays a function of past actions only; board information affects its own prediction position and behavior gradients remain stopped at both encoders. These prediction gains did not pass the real KataGo criterion. Any future speculative decoder must check the board input associated with each consumed own-policy prediction. Merely checking its selected move's legality does not preserve this state-conditioned model's policy. A cheap place-stone proposal can be tested with exact Rust input checks, stopping when captures or other state changes invalidate a future prediction. This execution idea is separate from the required endgame/strength work.

4. Test richer behavior conditioning on deliberately heterogeneous opponents. The current action-count adapter contributes only about0.003 nats. A private causal adapter can summarize opponent decisions relative to board context, while its gradients remain separated from expert policy/value. A mixed league of frozen historical checkpoints and search budgets provides explicit weak/strong populations without requiring KataGo training labels. Evaluate fresh held-out opponents and games, reporting calibration and log loss by predeclared level groups. Preserve observed-action targets even for weak players; do not substitute search-optimal labels in the behavior objective.

5. Completed: native pending-leaf histories bind actual root moves and hypothetical suffixes to search/network tickets. Rust and Python binding tests independently reconstruct deeper PUCT and Gumbel leaves, with stale-ticket rejection. `eval/causal_gtp.py` uses the original expert policy/value heads at every MCTS leaf, never the behavior predictor, and reserves the entire possible search suffix inside its context limit. All four real KataGo integration games completed with exact boards/scores; all were losses. See `../causal_mcts`. Future root encoding/KV reuse must preserve this baseline. Policy-only multi-step acceptance cannot silently substitute for MCTS decisions; any amortized fast-policy/slow-search variant needs its own strength study.

Promotion requires real KataGo benchmarks on the same rules, paired openings/colors, capped-game accounting, at least an independent training seed, and matched total inference/time budgets. An inference microbenchmark, lower NLL or fewer host exchanges is insufficient. Preserve all teacher compute and engineering/idle reservation cost when comparing end-to-end efficiency.

[project]
name = "gozero"
version = "0.1.0"
description = "Shared Go environment, execution, and research harness support"
requires-python = "==3.12.*"
dependencies = []

[project.scripts]
gozero = "gozero.__main__:main"

[build-system]
requires = ["uv_build==0.12.3"]
build-backend = "uv_build"
